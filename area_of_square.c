// AREA OF A CIRCLE
#include <stdio.h>
#include <math.h>

int main()
{
    int side;
    printf("Enter the side of the square: ");
    scanf("%d", &side);
    printf("The are of the circle is : %f", pow(side, 2));
    return 0;
}
